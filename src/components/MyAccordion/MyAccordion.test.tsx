import React from "react";
import MyAccordion from "./MyAccordion"

// describe("MyAccordion test cases", () => {
  
  // test("Developer can provide summary from props", () => {})
  // test("Developer can provide detail from props", () => {})
  // test("Developer can provide expandIcon from props", () => {})
  // test("Developers should be able to control the expand/retract state of the component from props", () => {})
  // test("Developer should be able to listen to the expand/retract change of the component by providing the onChange callback function as a prop", () => {})
  // test("User can expand/retract the component by clicking on component title", () => {})
  // test("User sees proper animation when the accordion expands/retracts", () => {})

// })



